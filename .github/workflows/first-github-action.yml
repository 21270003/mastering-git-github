name: 첫번째 GitHub Action
on:
    push:
        branches:
            - main

jobs:
  My-Deploy-Job:
    runs-on: ubuntu-latest
    steps:
      - name: 01-th Step - Linux 명령어
        run: |
          echo "Hello World" 
          echo "GitHub Action"
          pwd
          ls -al